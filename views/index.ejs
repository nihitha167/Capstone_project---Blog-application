<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Capstone Project - My Blog Application</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1><%= title %></h1>
  <a href="/new" class="add-post-button">Add New Post</a>
  <ul>
    <% posts.reverse().forEach(post => { %>
      <li>
        <div class="post-card">
          <h2><a href="/post/<%= post.id %>"><%= post.title %></a></h2>
          <p class="content-snippet"><%= post.content.substring(0, 150) %>...</p>
          <p><%= post.date %></p>
          <p>@<%= post.author %></p>
          <form action="/delete/<%= post.id %>" method="post">
            <button type="submit">Delete</button>
          </form>
        </div>
      </li>
    <% }); %>
  </ul>
</body>
</html>
